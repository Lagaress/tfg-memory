Antes de describir cada técnica de generación, conviene establecer el marco experimental que todas comparten. De este modo, los capítulos siguientes pueden centrarse exclusivamente en las decisiones de diseño de cada enfoque, sin repetir el contexto experimental.

\section{Descripción general del sistema}
\label{sec:descripcion-sistema}

La arquitectura de Dixi sigue un diseño modular: la entrada ---una lista ordenada de etiquetas de pictogramas--- atraviesa un motor de generación de lenguaje natural que produce una oración gramaticalmente correcta, y un motor de síntesis de voz la convierte en audio.

\textit{(TODO: Añadir diagrama de la arquitectura modular: pictogramas $\rightarrow$ motor de generación $\rightarrow$ oración $\rightarrow$ TTS $\rightarrow$ audio)}

El dominio de la aplicación está deliberadamente acotado. El vocabulario consta de 232 pictogramas ARASAAC organizados en nueve categorías cromáticas, una cantidad seleccionada para cubrir las necesidades comunicativas básicas del día a día (pedir necesidades, expresar emociones, hacer preguntas, describir estados, referirse a personas cercanas y lugares habituales) sin desbordar el alcance del proyecto. Este dominio controlado, con frases cortas y un conjunto cerrado de conceptos, es una decisión de diseño que permite explorar y comparar técnicas de generación sobre un problema acotado, sin las complejidades del lenguaje abierto.

Esta restricción abre una pregunta concreta: ¿qué técnica de generación se adapta mejor a un entorno donde la fidelidad al contenido pictográfico, la latencia y la posibilidad de funcionar sin conexión son más relevantes que la capacidad de manejar un vocabulario abierto?

Para responderla, se han implementado seis motores independientes que reciben exactamente la misma entrada y producen el mismo tipo de salida. Tres son técnicas base (gramática formal, embeddings semánticos y modelos de lenguaje) y tres son hibridaciones que combinan las anteriores: gramática con post-edición LLM, embeddings con post-edición LLM, y un sistema de sobregeneración y ranking.

La comparación directa entre los seis permite identificar qué enfoque, o combinación de enfoques, ofrece el mejor compromiso para un sistema de CAA real.

\section{Conjunto de datos de evaluación}
\label{sec:dataset}

La evaluación de los seis motores se realiza sobre un corpus de 428 casos de prueba diseñado específicamente para este proyecto. Cada caso consiste en una secuencia de pictogramas de entrada y la oración de referencia esperada como salida.

\subsection{Estructura y categorías}

Los 428 casos se distribuyen en ocho categorías semánticas que cubren los principales patrones comunicativos de un usuario de CAA:

\begin{table}[H]
\centering
\begin{tabular}{l l r}
\hline
Categoría & Descripción & Casos \\
\hline
Necesidad & Expresiones de deseo o necesidad & 73 \\
Compuesta & Coordinación de elementos & 61 \\
Interrogativa & Preguntas con partícula interrogativa & 61 \\
Estado & Estados físicos o emocionales & 55 \\
Negativa & Oraciones con negación & 52 \\
Movimiento & Desplazamiento a lugares & 50 \\
Descripción & Atributos de personas u objetos & 41 \\
Concordancia & Concordancia de género y número & 35 \\
\hline
\textit{Total} & & \textit{428} \\
\hline
\end{tabular}
\caption{Distribución de los casos de prueba por categoría semántica.}
\label{tab:dataset-categorias}
\end{table}

El corpus contiene 192 casos simples, 181 de complejidad media y 55 complejos, con longitudes de entrada que van desde un solo pictograma (17 casos) hasta cinco pictogramas (12 casos), pasando por dos (102 casos), tres (239 casos) y cuatro pictogramas (58 casos).

Cada categoría presenta desafíos lingüísticos específicos. Las oraciones de necesidad requieren inferir el verbo modal \textit{querer} cuando el usuario omite la intención; las de movimiento exigen seleccionar la preposición correcta según el lugar de destino; las interrogativas demandan invertir el orden sujeto-verbo; y las compuestas necesitan coordinación con conjunciones.

\subsection{Formato de los casos}

Cada caso de prueba se define como un par (entrada, referencia). La entrada es una lista de cadenas que representan las etiquetas de los pictogramas seleccionados por el usuario, y la referencia es la oración esperada en español natural. Por ejemplo:

\begin{lstlisting}[language={},caption={Ejemplo de caso de prueba del corpus de evaluación.}]
Entrada:    ["agua"]
Referencia: "Quiero agua"

Entrada:    ["yo", "querer", "comer", "manzana"]
Referencia: "Yo quiero comer una manzana"

Entrada:    ["donde", "estar", "mama"]
Referencia: "¿Donde esta mama?"
\end{lstlisting}

Nótese que la entrada puede variar en longitud desde un solo pictograma ---caso en el que el sistema debe inferir elementos omitidos--- hasta secuencias de cuatro o más pictogramas donde la tarea consiste principalmente en añadir los elementos funcionales (artículos, preposiciones, conjugaciones) que los pictogramas no codifican.

\subsection{Criterios de diseño del corpus}

El corpus se ha construido siguiendo tres criterios. La representatividad: las categorías reflejan las necesidades comunicativas más frecuentes de los usuarios de CAA, priorizando las expresiones de necesidad y deseo que constituyen la base de la comunicación funcional. La gradualidad: dentro de cada categoría se incluyen casos de complejidad creciente, desde entradas mínimas que exigen máxima inferencia hasta secuencias completas donde el sistema solo debe aplicar morfología. Y la cobertura gramatical: el conjunto abarca los principales fenómenos del español relevantes para CAA ---concordancia de género, contracciones, selección de preposiciones, interrogativas, negación y coordinación.

\section{Métricas de evaluación}
\label{sec:metricas}

La evaluación emplea un conjunto amplio de métricas organizadas en tres categorías: métricas con referencia, que comparan la predicción contra la oración gold standard; métricas sin referencia, que evalúan la calidad intrínseca del texto generado; y métricas específicas de CAA, que miden la fidelidad al contenido pictográfico de entrada. Además, se registran medidas de rendimiento operativo.

\subsection{Exact Match}

La métrica más estricta: compara la predicción con la referencia carácter a carácter, tras normalizar a minúsculas y eliminar espacios sobrantes. Devuelve 1 si la oración generada es idéntica a la referencia y 0 en cualquier otro caso. Su valor reside en que una coincidencia exacta garantiza corrección completa, pero su limitación es que penaliza por igual una variación menor (un artículo de más) y una oración completamente incorrecta.

$$\text{EM}(p, r) = \begin{cases} 1 & \text{si } \text{lower}(\text{strip}(p)) = \text{lower}(\text{strip}(r)) \\ 0 & \text{en otro caso} \end{cases}$$

\subsection{BLEU}

BLEU (\textit{Bilingual Evaluation Understudy}) mide la precisión de n-gramas entre la predicción y la referencia. Se calculan BLEU-1 (unigramas) y BLEU-4 (hasta 4-gramas) mediante sacreBLEU con el parámetro \texttt{effective\_order=True}, que ajusta el orden efectivo de n-gramas para oraciones cortas: en lugar de aplicar un suavizado artificial, calcula la media geométrica solo sobre los órdenes de n-gramas que existen en la oración. Así, una oración de tres palabras se evalúa con BLEU-3 efectivo en lugar de recibir un cero en 4-gramas que penalizaría artificialmente el resultado.

BLEU-1 captura la adecuación léxica ---si las palabras correctas están presentes---, mientras que BLEU-4, al considerar secuencias más largas, refleja también la fluidez y el orden de las palabras. Ambas métricas se complementan: una predicción puede obtener BLEU-1 alto pero BLEU-4 bajo si contiene las palabras correctas en un orden incorrecto.

\subsection{ROUGE-L}

ROUGE-L se basa en la subsecuencia común más larga (\textit{Longest Common Subsequence}, LCS) entre la predicción y la referencia. A diferencia de BLEU, que requiere n-gramas consecutivos, ROUGE-L permite omisiones entre las palabras coincidentes, lo que la hace más tolerante con variaciones en el orden.

A partir de la longitud de la LCS se calculan precisión, recall y su media armónica $F_1$:

$$\text{precisión} = \frac{\text{LCS}}{|\text{pred}|}, \quad \text{recall} = \frac{\text{LCS}}{|\text{ref}|}$$

$$F_1 = \frac{2 \cdot \text{precisión} \cdot \text{recall}}{\text{precisión} + \text{recall}}$$

ROUGE-L resulta especialmente útil en este contexto porque captura la estructura secuencial de la oración sin penalizar la inserción de elementos adicionales (como artículos que el sistema añade correctamente pero que no figuran en la referencia de la misma forma).

\subsection{METEOR}

METEOR (\textit{Metric for Evaluation of Translation with Explicit ORdering}) calcula la media armónica de la precisión de unigramas y el recall, aplicando después una penalización por fragmentación que castiga las coincidencias dispersas frente a las contiguas. A diferencia de BLEU, que solo mide precisión, METEOR incorpora el recall directamente en su formulación, lo que la hace más sensible a omisiones.

$$\text{METEOR} = F_\text{mean} \cdot (1 - \text{penalty})$$

donde $F_\text{mean}$ es la media armónica ponderada de precisión y recall de unigramas, y la penalización crece con el número de fragmentos no contiguos en la alineación. La implementación utiliza la librería NLTK con tokenización por espacios, apropiada para el español donde los tokens están bien delimitados por espacios en blanco.

METEOR resulta complementaria a BLEU porque captura tanto la presencia de las palabras correctas como su contigüidad en la oración generada. Su valor ligeramente inferior a 1,0 incluso en coincidencias exactas (típicamente $\sim$0,97) se debe al mecanismo de penalización por fragmentación, que no otorga un score perfecto incluso cuando todos los unigramas coinciden.

\subsection{chrF++}

chrF++ evalúa la similitud entre predicción y referencia combinando n-gramas de caracteres con bigramas de palabras. Los n-gramas de caracteres capturan coincidencias parciales a nivel morfológico ---por ejemplo, reconocen la similitud entre \textit{quiero} y \textit{quiere} aunque difieran en su terminación---, mientras que los bigramas de palabras incorporan información sobre el orden.

$$\text{chrF++} = (1 + \beta^2) \cdot \frac{\text{chrP} \cdot \text{chrR}}{\beta^2 \cdot \text{chrP} + \text{chrR}}$$

donde chrP y chrR son la precisión y recall calculadas sobre n-gramas de caracteres y bigramas de palabras conjuntamente, con $\beta = 2$ por defecto. Esta métrica resulta especialmente útil para lenguas con morfología rica como el español, donde variaciones flexivas que las métricas de palabras completas penalizan severamente ---como la diferencia entre \textit{come} y \textit{comer}--- representan divergencias menores a nivel de caracteres. La implementación utiliza sacreBLEU con el parámetro \texttt{word\_order=2}.

\subsection{Cobertura de pictogramas}

La cobertura mide la fracción de pictogramas de entrada que se encuentran representados en la oración generada. Para determinar si un pictograma está cubierto, se comparan sus lemas ---obtenidos mediante el modelo de lematización de spaCy para español--- con los lemas de las palabras de la oración de salida. Si existe al menos un lema en común, el pictograma se considera cubierto.

$$\text{Cobertura} = \frac{|\text{pictogramas cubiertos}|}{|\text{pictogramas de entrada}|}$$

Por ejemplo, dado el pictograma \textit{comer} (lema: \textit{comer}) y la oración generada \textit{Yo como pan} (lemas: \textit{yo}, \textit{comer}, \textit{pan}), el pictograma se considera cubierto porque el lema \textit{comer} aparece en ambos conjuntos. Esta métrica es específica del dominio de CAA y complementa las métricas de coincidencia con la referencia: mientras que BLEU o ROUGE-L comparan contra una oración gold standard, la cobertura verifica directamente que el contenido pictográfico se preserve en la salida, independientemente de la formulación exacta.

\subsection{BERTScore}

BERTScore evalúa la similitud semántica entre la predicción y la referencia utilizando embeddings contextuales de un modelo BERT preentrenado en español (\texttt{dccuchile/bert-base-spanish-wwm-cased}). A diferencia de las métricas anteriores, que operan a nivel de tokens o n-gramas, BERTScore captura equivalencias semánticas ---por ejemplo, reconoce que \textit{deseo} y \textit{quiero} expresan significados cercanos aunque no compartan tokens.

El cálculo alinea cada token de la predicción con su token más similar en la referencia mediante similitud coseno en el espacio de embeddings, y obtiene precisión, recall y $F_1$. Se utiliza un modelo BERT preentrenado en español en lugar del modelo por defecto en inglés, lo que mejora la captura de matices semánticos propios del castellano. Esta métrica se aplica a los seis motores de generación, siendo especialmente informativa cuando los motores producen paráfrasis válidas que las métricas de coincidencia exacta penalizarían.

\subsection{Evaluación multi-referencia}
\label{sec:multi-referencia}

Las métricas con referencia descritas hasta aquí comparan cada predicción contra una única oración de referencia. Esta estrategia penaliza las reformulaciones válidas: si un motor genera \textit{Quiero beber agua} y la referencia es \textit{Quiero agua}, métricas como Exact Match o BLEU-4 registran una penalización a pesar de que ambas oraciones son correctas. La evaluación multi-referencia mitiga esta limitación utilizando las salidas de los seis motores como referencias alternativas para cada caso.

El cálculo varía según la métrica. Para BLEU y chrF++, sacreBLEU ofrece soporte nativo de múltiples referencias: el score de un motor se calcula contra las salidas de los otros cinco motores simultáneamente, seleccionando la referencia que maximiza la precisión de n-gramas. Para METEOR, ROUGE-L y BERTScore, que aceptan una única referencia, se adopta una estrategia de máximo: se calcula el score contra cada una de las cinco referencias alternativas y se retiene el valor más alto. Para Exact Match, basta con que la predicción coincida con al menos una de las cinco referencias alternativas.

Esta metodología introduce una circularidad parcial ---las salidas de un motor se usan como referencia para otro---. La mitigación principal es que cada motor se evalúa exclusivamente contra los otros cinco, nunca contra sí mismo. La convergencia observada entre motores (véase la Sección~\ref{sec:eval-convergencia}) reduce la circularidad a un efecto de segundo orden: cuando los seis motores producen la misma salida en el 80,4\% de los casos, las referencias alternativas son en su mayoría idénticas a la referencia original. La evaluación multi-referencia resulta informativa precisamente en el 19,6\% restante, donde los motores divergen y la métrica permite distinguir entre reformulaciones válidas y errores genuinos.

\subsection{Normalización previa al cálculo}

Antes de calcular las métricas con referencia (Exact Match, BLEU, ROUGE-L, METEOR, chrF++ y BERTScore), el pipeline normaliza la puntuación terminal de hipótesis y referencia eliminando los signos de puntuación terminal: punto final, signos de exclamación e interrogación tanto de apertura como de cierre. Esta normalización responde a un efecto observado en los motores híbridos: el post-editor GPT-4o añade sistemáticamente un punto final a las frases que genera, mientras que las referencias del corpus no incluyen puntuación terminal. Sin la normalización, los híbridos recibirían una penalización artificial en todas las métricas de coincidencia por un carácter de puntuación irrelevante para la calidad lingüística. La normalización no se aplica a la perplejidad ni a las métricas específicas de CAA (cobertura, fidelidad), que operan sobre el texto tal como lo produce cada motor.

\subsection{Fidelidad de contenido}

La fidelidad de contenido mide la proporción de palabras de contenido en la oración generada que provienen efectivamente de los pictogramas de entrada, detectando así las \textit{alucinaciones}: palabras que el motor introduce sin que exista un pictograma que las justifique.

$$\text{Fidelidad} = 1 - \frac{|\text{palabras alucinadas}|}{|\text{palabras de contenido generadas}|}$$

Se excluyen del cálculo las \textit{stop words} ---artículos, preposiciones, pronombres, conectores y verbos auxiliares--- para evitar penalizar los elementos funcionales que el sistema añade legítimamente al construir la oración a partir de los pictogramas.

La fidelidad complementa la cobertura: mientras la cobertura detecta omisiones (pictogramas de entrada que no aparecen en la salida), la fidelidad detecta adiciones (palabras de contenido en la salida que no provienen de la entrada). Un motor ideal obtendría cobertura y fidelidad de 1,0 simultáneamente.

\subsection{Fidelidad ajustada}

La fidelidad de contenido descrita en la subsección anterior penaliza como alucinación toda palabra de contenido que no corresponda a un pictograma de entrada. Esta definición operativa produce falsos positivos en un caso recurrente en CAA: la inferencia pragmática. Cuando un usuario selecciona únicamente el pictograma \textit{agua}, el motor puede inferir legítimamente el verbo \textit{querer} y generar \textit{Quiero agua}. La palabra \textit{quiero} no aparece entre los pictogramas de entrada, de modo que la fidelidad estándar la contabiliza como alucinación, aunque se trata de una inferencia que cualquier interlocutor humano haría rutinariamente.

La fidelidad ajustada corrige este efecto excluyendo del recuento de alucinaciones aquellas palabras que constituyen inferencias pragmáticas ---verbos modales como \textit{querer}, verbos de acción predecibles por el contexto y otros elementos que los usuarios de CAA omiten sistemáticamente por economía comunicativa---. El cálculo es idéntico al de la fidelidad estándar, pero el conjunto de palabras alucinadas se reduce al descontar las inferencias detectadas:

$$\text{Fidelidad ajustada} = 1 - \frac{|\text{palabras alucinadas}| - |\text{inferencias pragmáticas}|}{|\text{palabras de contenido generadas}|}$$

La diferencia entre ambas métricas cuantifica el peso de las inferencias pragmáticas en cada motor. Un motor con fidelidad baja pero fidelidad ajustada alta alucina poco en sentido estricto: la mayoría de sus ``alucinaciones'' son inferencias justificables por el contexto comunicativo.

\subsection{Perplejidad y pseudo-perplejidad}

La perplejidad y la pseudo-perplejidad son métricas sin referencia que evalúan la naturalidad del texto generado sin necesidad de compararlo contra una oración gold standard. Ambas miden cuán probable considera un modelo de lenguaje preentrenado la oración generada: valores menores indican texto más natural y fluido.

La perplejidad se calcula con un modelo GPT-2 entrenado en español (\texttt{DeepESP/gpt2-spanish}), un modelo autoregresivo que estima la probabilidad de cada palabra condicionada a las anteriores:

$$\text{PPL}(s) = 2^{-\frac{1}{N} \sum_{i=1}^{N} \log_2 P(w_i \mid w_1, \ldots, w_{i-1})}$$

La pseudo-perplejidad utiliza un modelo BERT enmascarado en español (\texttt{dccuchile/bert-base-spanish-wwm-cased}), que evalúa la probabilidad de cada token dado su contexto bidireccional. Para ello, se enmascara cada token de la oración y se mide la probabilidad de reconstruirlo a partir del resto. Este modelo es el mismo que se emplea para el cálculo de BERTScore, lo que permite reutilizar la carga del modelo y reducir el tiempo total de evaluación.

Estas métricas son especialmente relevantes en el contexto de CAA porque capturan la fluidez percibida del texto independientemente de la referencia. Un motor puede generar una oración semánticamente correcta pero con una estructura gramatical poco natural, algo que las métricas de coincidencia con la referencia no detectan pero que la perplejidad sí penaliza.

\subsection{Análisis de errores}

El análisis de errores clasifica cada caso en una de cinco categorías mediante heurísticas basadas en los valores de cobertura y fidelidad ya calculados:

\begin{itemize}
    \item \textit{Ninguno}: la generación coincide con la referencia o no presenta errores detectables.
    \item \textit{Omisión}: uno o más pictogramas de entrada no se reflejan en la salida (cobertura inferior a 1,0).
    \item \textit{Alucinación}: la oración contiene palabras de contenido no justificadas por los pictogramas (fidelidad inferior a 0,8).
    \item \textit{Morfológico}: las palabras correctas aparecen pero con forma flexiva diferente ---conjugación, género o número incorrectos--- (solapamiento léxico superior al 50\% sin coincidencia exacta).
    \item \textit{Léxico}: el motor elige palabras diferentes para expresar el mismo concepto (solapamiento léxico inferior al 50\% con cobertura parcial).
\end{itemize}

Esta clasificación no es mutuamente excluyente: un mismo caso puede presentar simultáneamente omisión y alucinación. El análisis de errores proporciona una visión cualitativa que complementa las métricas numéricas, permitiendo identificar no solo cuánto se equivoca cada motor sino cómo se equivoca.

\subsection{Métricas operativas}

Además de la calidad lingüística, se miden dos aspectos operativos relevantes para un sistema de CAA:

\begin{itemize}
    \item \textit{Latencia}: tiempo de procesamiento por consulta, medido con temporizadores de alta resolución. En un sistema de comunicación, la latencia percibida por el usuario afecta directamente la fluidez de la interacción.
    \item \textit{Coste}: aplicable únicamente a la Fase 3, que utiliza una API comercial de pago. Se estima a partir del número de tokens procesados por consulta y las tarifas del proveedor.
\end{itemize}

\section{Entorno tecnológico}
\label{sec:entorno}

Los tres motores se implementan en Python 3.10+ y comparten la estructura de evaluación, pero difieren sustancialmente en sus dependencias:

\begin{table}[H]
\centering
\begin{tabular}{l l l}
\hline
Fase & Dependencias principales & Requisitos de hardware \\
\hline
Fase 1 & Ninguna (biblioteca estándar) & CPU \\
Fase 2 & FastText (modelo cc.es.300.bin, 4 GB) & CPU, 6 GB RAM \\
Fase 3 & API OpenAI (GPT-4o / GPT-4o-mini) & Conexión a Internet \\
\hline
\end{tabular}
\caption{Dependencias y requisitos por fase.}
\label{tab:dependencias}
\end{table}

La progresión en dependencias refleja un compromiso recurrente en el diseño del sistema: la Fase 1 funciona en cualquier entorno con Python instalado, la Fase 2 requiere descargar un modelo de embeddings de 4 GB y disponer de memoria suficiente para cargarlo, y la Fase 3 necesita conexión a Internet y una clave de API con coste asociado. Esta gradación en requisitos es relevante para el despliegue real en dispositivos de CAA, donde la conectividad no siempre está garantizada.

La evaluación se ejecuta en un entorno unificado para garantizar la comparabilidad: un equipo con procesador Apple M1, 16 GB de RAM y macOS, utilizando Python 3.12. Las mediciones de latencia se realizan en condiciones equivalentes, con la salvedad de que la Fase 3 incluye el tiempo de red de la llamada a la API.
